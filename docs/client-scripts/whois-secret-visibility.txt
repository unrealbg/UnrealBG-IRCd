# WHOIS secret channel visibility
#
# Setup assumptions:
# - Two clients: Req (requester) and Target.
# - Channel #s is set +s (secret).

# As Target:
C: JOIN #s
C: MODE #s +s

# As Req (NOT in #s):
C: WHOIS Target
S: :server 319 Req Target :<should NOT include #s>

# As Req (join #s):
C: JOIN #s
C: WHOIS Target
S: :server 319 Req Target :<should include #s>

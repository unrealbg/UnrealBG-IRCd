flood {
    client {
        maxlines = 20;
        window = 10;
    };

    // Command-level flood control (enforced by CommandDispatcher)
    commands {
        enabled = true;
        violations_before_disconnect = 3;
        warning_cooldown = 5;
        violation_reset_seconds = 60;
        exempt_opers = true;
        oper_multiplier = 4;
        temp_dline_enabled = false;
        temp_dline_minutes = 10;

        messages {
            enabled = true;
            maxevents = 10;
            window = 10;
            per_target = true;
        };

        joinpart {
            enabled = true;
            maxevents = 10;
            window = 30;
        };

        whowhois {
            enabled = true;
            maxevents = 10;
            window = 10;
        };

        mode {
            enabled = true;
            maxevents = 10;
            window = 10;
        };

        nick {
            enabled = true;
            maxevents = 5;
            window = 60;
        };
    };

    tlsclient {
        maxlines = 20;
        window = 10;
    };

    serverlink {
        maxlines = 50;
        window = 10;
    };
};
